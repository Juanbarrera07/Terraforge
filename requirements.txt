# TerraForge — pip-only packages (pure Python, no native geo libs)
# Install AFTER requirements-conda.txt has been applied.
# Install with:
#   pip install -r requirements.txt
#
# NEVER add gdal, rasterio, fiona, shapely, pyproj, geopandas here.

# ── Phase 1–2 (active) ───────────────────────────────────────────────────────
streamlit>=1.33.0
pytest>=8.0.0
pytest-cov>=4.1.0

# ── Phase 3+ ──────────────────────────────────────────────────────────────────
# plotly>=5.19.0
# folium>=0.16.0
# streamlit-folium>=0.18.0

# ── Phase 5 ───────────────────────────────────────────────────────────────────
# imbalanced-learn>=0.12.0

# ── Phase 6 ───────────────────────────────────────────────────────────────────
# whitebox>=2.3.0       # pure-Python binary wrapper — pip is safe

# ── Phase 7 ───────────────────────────────────────────────────────────────────
# rio-cogeo>=5.3.0
# pystac>=1.9.0
# reportlab>=4.1.0
